terraform {
  required_version = ">= 1.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
  
  # Note: This terraform configuration uses LOCAL state
  # because it's creating the backend for OTHER configurations
}