spring:
  cloud:
    aws:
      region:
        static: ${AWS_REGION}
      
app:
  s3:
    bucket-name: ${S3_BUCKET_NAME}
  dynamodb:
    image-table-name: ${DYNAMODB_TABLE_NAME}

# Management endpoints for health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      enabled: true
      show-details: when-authorized