spring:
  cloud:
    aws:
      region:
        static: us-east-1 # Force 'us-east-1' for LocalStack
      credentials:
        access-key: dummy-key
        secret-key: dummy-secret-key

app:
  s3:
    bucket-name: test-bucket
  dynamodb:
    image-table-name: setadvancedtable